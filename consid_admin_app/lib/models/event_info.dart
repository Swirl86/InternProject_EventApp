class EventInfo {
  String title;
  String description;
  DateTime endDate;
  DateTime startDate;

  EventInfo(
      {required this.title,
      required this.description,
      required this.endDate,
      required this.startDate});
}
